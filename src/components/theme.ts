import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#004F8A',
      light: '#004F8A',
      dark: '#004F8A',
    },
    secondary: {
      main: "#8a99b4",
      light: "#bac3d3",
      dark: "#63779b",
    }
  },
});

export default theme;
